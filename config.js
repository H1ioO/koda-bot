require('dotenv').config();
const { Colors } = require("discord.js");

module.exports = {
    token: process.env.TOKEN,
    prefix: "",
    color: Colors.Purple,
    owner: [
        "ID OWNER"
    ]
}